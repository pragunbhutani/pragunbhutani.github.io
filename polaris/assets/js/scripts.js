$(document).ready(function() {

	// var featureTextItems = $(".feature-text");
	// var activeIndex = 0;
	// setInterval(function() {
	// 	var previousIndex = activeIndex;
	// 	if (activeIndex != 2) {
	// 		activeIndex++;
	// 	} else {
	// 		activeIndex = 0;
	// 	}
	// 	$(featureTextItems[previousIndex]).removeClass('active');
	// 	$(featureTextItems[activeIndex]).addClass('active');
	// }, 4000);

});